Flask>=2.0
Flask-SQLAlchemy>=2.5
psycopg2-binary>=2.9 # PostgreSQL adapter
python-dotenv>=0.19 # For loading .env file
google-auth>=2.0    # For Google token verification
google-auth-oauthlib>=0.4 # Although we primarily use google-auth for verification
requests>=2.25     # google-auth dependency
Werkzeug>=2.0      # Flask dependency, ensure compatibility
itsdangerous>=2.0  # Flask dependency
Flask-Cors>=3.0    # To handle Cross-Origin Resource Sharing if frontend is on different origin
SQLAlchemy>=1.4    # Ensure compatibility
